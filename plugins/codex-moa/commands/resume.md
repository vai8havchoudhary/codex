---
description: Resume a native Codex MoA run from an opaque checkpoint after reconciling repository and model state.
---

Use the `codex-moa` skill resume procedure. Read the supplied checkpoint handle, re-check repository authority and exact model admission, inspect the current worktree, revalidate the last milestone when state changed, and continue only from reconciled evidence.
