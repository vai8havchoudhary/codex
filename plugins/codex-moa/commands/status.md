---
description: Inspect native Codex MoA model admission and compact checkpoint state without mutating the repository.
---

Run the packaged preflight in read-only mode and use `checkpoint_list` or `checkpoint_get` for the requested run. Report exact model IDs, checkpoint phase/status, last validation evidence, retry budget, risks, and next action. Never print environment values or proxy account information.
