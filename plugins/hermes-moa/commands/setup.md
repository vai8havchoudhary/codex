---
description: Configure both Hermes Agent MoA presets through one CLIProxyAPI provider.
---

Invoke the `hermes-moa` skill. Use `$ARGUMENTS` to choose `grok-led` or `gemini-led` (default `grok-led`), run the bundled transactional setup entry point once, and report only non-secret route and backup metadata.
