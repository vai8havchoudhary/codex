---
description: Run a one-shot Hermes Mixture-of-Agents request with the configured default preset.
---

Invoke the `hermes-moa` skill. Verify status first, then tell the user to run `/moa $ARGUMENTS` in Hermes. Do not execute or claim a Hermes response from Codex unless the user has provided a Hermes execution surface.
